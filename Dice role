import math
y, w = map(int, input().split())
highest_roll = max(y, w)
numerator = 6 - highest_roll + 1
denominator = 6
common_divisor = math.gcd(numerator, denominator)
print(f"{numerator // common_divisor}/{denominator // common_divisor}")
